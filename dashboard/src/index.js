/** @format */

import React from 'react';
import ReactDOM from 'react-dom';
import './resets.css';

ReactDOM.render(
	<React.StrictMode>
		<App />
	</React.StrictMode>,
	document.getElementById('root')
);
